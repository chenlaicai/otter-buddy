# Dependencies
node_modules

# Build output
dist/

# Environment variables
.env
.env.local

# Local config (managed by user, not tracked)
config/config.yaml
config/config.test.local.yaml

# IDE
.vscode/
.idea/
.claude/settings.local.json

# Local agent config (managed by user, not tracked)
CLAUDE.md
AGENTS.md
opencode.json
agents.md
.claude/
.agents/
.codex/
.gemini/
.kimi/
.mcp.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
logs/
pids
*.pid
*.seed
*.pid.lock

# Coverage
coverage/
*.lcov
.nyc_output

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Temporary folders
tmp/
temp/

# Runtime files
.otter-buddy.pid

# Database files
data/*.db
*.db
*.db-shm
*.db-wal

# Agent sessions
data/sessions/

# Inbound media attachments (user-uploaded images/voice/files — runtime user data,
# introduced by multimodal attachments pipeline F20260827mmdu)
data/attachments/

# Weixin channel runtime state (bot_token credentials, context tokens, sync
# cursor — credentials must never enter git; introduced by weixin channel #569)
data/weixin/

# Metrics runtime output (prom-client JSONL snapshots, 7-day rotation)
data/metrics/

# Per-conversation workspace dirs (runtime scratch area for agent tools)
data/workspaces/

# Local model weights (large, downloaded separately)
# Layout: models/<model-name>/onnx/*.onnx[+_data]
models/
*.bak

# Golden 场景结果沉淀（本地数据，非 git 追踪）
tests/capability/golden/results.jsonl

# Python venv (stock-cli)
.venv-stock/

# stock-cli cache
.cache/stock/
