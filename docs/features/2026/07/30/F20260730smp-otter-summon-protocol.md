---
id: F20260730smp
title: otter-summon-protocol
doc_type: feature

summary: |
  大獭召唤小獭的行为协议：召唤决策框架、systemPrompt 模板、多轮协作编排、产出接住。
  信道分层：身份层（BIG_OTTER.md）只保留一句话声明，具体 know-how 放在 otter-summon skill（按需加载）。
  小獭产出标准化：SMALL_OTTER.md 增加 speak 发言三要素（硬规则，留在身份层）。
  本特性记录了"信道选择"的认知教训——与 F20260724skch 是对称错误。

causal_links:
  from:
    - F20260721cap    # capability-oriented-skills：能力导向 skill 为小獭提供了能力基础
    - F20260720qs9y   # per-conversation-otter：每对话独立大獭是召唤的前提
    - F20260724skch   # skill-tool-channel-consolidation：信道分层原则（本特性的设计依据）

status: draft
change_type: prompt
tags: [agent, prompt, otter, summon, collaboration, identity, skill, channel]
modules:
  - prompts/identity/BIG_OTTER.md
  - prompts/identity/SMALL_OTTER.md
  - .pi/skills/otter-summon/

created_at: 2026-07-30
---

# F20260730smp 大獭召唤小獭协议

## 背景

### 问题

大獭拥有 `create_otter` 和 `dissolve_otter` 工具，但缺乏使用这些工具的行为指引：

| 缺口 | 表现 |
|------|------|
| **决策框架缺失** | BIG_OTTER.md 只说"碰上硬活儿就创建小獭"，没定义什么是硬活儿、怎么判断 |
| **systemPrompt 是裸接口** | 大獭需要从零写 systemPrompt，没有模板，导致小獭能力参差不齐 |
| **产出无标准** | 小獭说完就走，但"说什么"没有规范，大獭无法可靠判断任务是否完成 |
| **多轮协作无模式** | 开发↔检视需要多轮对话，但大獭不知道如何编排发言石路由链条 |

根因：`create_otter` 是纯机制层工具，行为层（何时用、怎么用、用完怎么收）完全靠 LLM 自由发挥。

### 信道分层决策

初版设计将召唤协议全部写入 BIG_OTTER.md（身份层，每次 invoke 必达）。用户指出这违反了 F20260724skch 建立的信道分层原则：

> | 内容性质 | 位置 | 可达性 |
> |------|------|------|
> | 无条件遵守的硬规则 | SYSTEM.md / tool description | 每次必达 |
> | 工具用法契约 | tool description | 每次必达 |
> | 复杂任务的程序化 know-how | **skill** | **按需可达** |

召唤协议是"复杂任务的程序化 know-how"——包含场景判断、模板、编排模式，属于按需加载的内容。放在身份层会导致：
1. 每次 invoke 都携带大量 token，即使本次对话不需要召唤
2. 与身份层"轻量、稳定"的定位矛盾
3. 违反 F20260724skch 的防再犯口诀："改行为规则前先问'这条规则在哪个信道上？'"

**最终方案**：身份层一句话声明 → skill 按需加载详细协议。

**触发机制设计**：otter-summon 的触发与其他 skill 不同——不是用户说了特定的话，而是大獭自己判断任务复杂度。这在 SDK 的 skill 懒加载机制下是循环依赖：大獭需要先读到场景表才能判断是否需要召唤，但场景表在 skill 里。解决方案是优化 skill 的 description，使其包含足够丰富的触发场景描述（需求分析、代码实现、方案检视、多角色讨论、并行调研、调试排查），让 LLM 能从任务性质直接匹配。身份层不堆砌触发指令——这属于 LLM 的能力范畴，不需要污染系统提示词。

### 认知教训：信道选择是反复出现的设计偏差

本特性的信道错放问题不是孤例。项目历史中，信道选择偏差出现过多次：

| 时间 | 特性 | 偏差方向 | 纠正 |
|------|------|---------|------|
| F20260720k7m2 | skill-injection-native | 1:1 skill 内容应在 tool description | 删除 5 个 1:1 skill，内容归位 tool description |
| F20260724skch | skill-tool-channel-consolidation | speak 规则在懒加载 skill（不可达） | 硬规则移到 SYSTEM.md，工具用法归位 tool description |
| **F20260730smp** | **otter-summon-protocol** | **know-how 在身份 prompt（每次必达）** | **拆到 skill，身份层只留一句话** |

**模式**：信道偏差有两种对称错误——
- 把必达规则放到懒加载 skill（模型看不到）→ F20260724skch
- 把按需 know-how 放到必达 prompt（token 浪费、层职责混淆）→ 本特性

**防再犯**：设计 prompt 改动时，先分类内容性质（硬规则？工具契约？程序化 know-how？），再选择信道。

## 用户意图锚

| ID | 用户原话 | 关键修饰语 | 架构师解读 | 来源 |
|----|---------|-----------|-----------|------|
| UA-1 | "小獭进场/退场能由大獭来自行决定" | 自行决定 | 召唤应是大獭的自主能力，不需要用户显式触发 | 本次对话 |
| UA-2 | "用户只抛出问题，然后大獭来解决，而小獭也是一种解决方式" | 小獭是解决方式 | 召唤是大獭解决问题的手段之一，不是独立流程 | 本次对话 |
| UA-3 | "设计/开发/检视，彼此间可能是需要多轮对话的" | 多轮对话 | 需要编排能力：开发↔检视循环、发言石路由 | 本次对话 |
| UA-4 | "otter 系统类似于提供了一个协作沟通的平台" | 协作平台 | 小獭不是 subagent，是协作参与者 | 本次对话 |
| UA-5 | "要先整理清楚 system prompt 和 skill/tool 的边界" | 信道边界 | 内容必须放在正确的信道上 | 本次对话 |

## 设计方案

### D1 — BIG_OTTER.md：身份层精简

两处改动：
1. "你是谁"段落删除"碰上专门的硬活儿，你就创建专门的小獭下水"——召唤相关描述统一收敛到"召唤小獭"段落，消除冗余
2. `## 召唤小獭` 段落从 59 行精简为 1 行：

> 你有权也有责任在需要时创建和管理小獭。召唤的判断、systemPrompt 编写、协作编排——见 `otter-summon` skill。

身份层职责：声明"你是谁"（有权召唤）。行为层职责：skill 教"怎么做"。

### D2 — otter-summon skill：按需加载的召唤协议

新增 `.pi/skills/otter-summon/SKILL.md`，包含：

**召唤场景表**（6 种典型场景）：

| 场景 | 小獭名字 | systemPrompt 要点 |
|------|---------|-------------------|
| 需求分析/方案设计 | 分析獭 | 指定需求、约束、背景，期望结构化方案 |
| 代码实现 | 开发獭 | 指定方案编号、实现范围、仓库和分支 |
| 代码/方案检视 | 检视獭 | 指定要检视的产出，期望结构化检视报告 |
| 模拟多角色讨论 | 按立场命名 | 指定角色立场和关注点 |
| 并行调研多个方向 | 按方向命名 | 指定调研方向和范围 |
| 调试/排查 | 排查獭 | 指定问题现象、已有线索、排查范围，期望根因分析 |

**触发机制**：skill description 包含丰富的触发场景描述（需求分析、代码实现、方案检视、多角色讨论、并行调研、调试排查），LLM 从任务性质直接匹配。

**召唤原则**（3 条）：
1. 召唤要有明确任务（systemPrompt 必须含四要素）
2. 传递上下文（不让小獭从零开始）
3. 小獭说完要接住（根据产出决定下一步）

**systemPrompt 模板**（四段式）：
```
你的任务：[一句话]
背景信息：[上下文、前置依赖、约束]
预期产出：[格式要求]
完成标准：[什么算做完]
```

**接住小獭**行为锚：审视产出 → 整合 → 补漏/再召唤 → 汇报搭档。

**references/collaboration-patterns.md**：多轮协作详细模式（开发↔检视循环、并行调研、角色讨论）。

### D3 — SMALL_OTTER.md：产出标准化（硬规则，留在身份层）

新增 `### 完成任务时` 段落（作为"你的边界"的子段落），要求小獭发言必须包含三要素：

1. **结论/产出**：核心交付物
2. **发现的问题**（如有）：职责外但值得大獭关注的问题
3. **建议**（如有）：对后续工作的具体建议

这是硬规则（每次发言都必须遵守），放在身份层（每次必达信道）是正确的。作为"你的边界"的子段落，形成"干完即走 → 走之前说什么"的逻辑流。

## 信道分层总结

| 内容 | 信道 | 理由 |
|------|------|------|
| "你有权召唤小獭" | BIG_OTTER.md（身份，必达） | 身份声明，稳定不变 |
| 召唤场景/原则/模板/编排 | otter-summon skill（按需） | 程序化 know-how，复杂任务才需要 |
| 小獭发言三要素 | SMALL_OTTER.md（身份，必达） | 硬规则，每次发言都必须遵守 |

## 改动清单

| 文件 | 改动 |
|------|------|
| `prompts/identity/BIG_OTTER.md` | +4 行：召唤身份声明（一句话）+ 删除"你是谁"中的召唤描述冗余 |
| `prompts/identity/SMALL_OTTER.md` | +10 行：完成任务时发言三要素 |
| `.pi/skills/otter-summon/SKILL.md` | 新增：召唤协议核心（场景、原则、模板、接住） |
| `.pi/skills/otter-summon/references/collaboration-patterns.md` | 新增：多轮协作编排模式 |

## 硬约束

1. 纯 prompt 改动，不改任何代码
2. 不改变 `create_otter` / `dissolve_otter` 工具的接口和行为
3. 不改变小獭的工具权限（仍为只读 + 协作工具）
4. 不改变 skill 注入机制（ResourceLoader 不变）
5. 身份层不膨胀——只保留声明，不放 know-how

## 设计取舍

| 取舍 | 决策 | 替代方案 | 理由 |
|------|------|---------|------|
| 召唤协议位置 | skill（按需加载） | 身份 prompt（每次必达） | 符合 F20260724skch 信道分层原则；身份层应轻量 |
| 场景粒度 | 6 种典型场景 | 只给原则不给场景 | 场景表降低 LLM 决策成本，但保留灵活性 |
| 触发机制 | skill description 包含场景描述 | 身份层堆砌触发指令 | 属于 LLM 能力范畴，不污染系统提示词 |
| 多轮协作位置 | skill references/ | skill 核心 or 身份 prompt | 协作模式是进阶内容，渐进披露 |
| 小獭产出规范 | 身份层硬规则 | 放在 skill | 每次发言都必须遵守，不是按需 |

## 验证

| 验证项 | 方法 |
|--------|------|
| 大獭能在适当时机主动召唤小獭 | 人工对话测试：抛出复杂需求，观察大獭是否自主创建小獭 |
| systemPrompt 包含四要素（任务/背景/产出/标准） | 检查 create_otter 调用的 systemPrompt 参数 |
| 小獭发言包含结论/问题/建议三要素 | 检查小獭 speak 调用的 body 内容 |
| 多轮协作模式能运转 | 人工对话测试：抛出需要开发+检视的任务，观察发言石路由 |
| 大獭接住小獭产出并整合 | 检查大獭在小獭发言后的后续行为 |
| 简单任务大獭不召唤 | 人工对话测试：抛出简单问题，确认大獭直接回答 |
| 身份层 token 不膨胀 | 对比改动前后 BIG_OTTER.md 字数 |

## 对抗检视记录

### 第一轮（agent 检视）：需修改

5 个维度共发现 12 个问题。核心问题：

| 优先级 | 问题 | 修复 |
|--------|------|------|
| P0 | skill description 缺触发短语，大獭无法匹配 → "见 otter-summon skill" 不可执行 | description 改为包含丰富的场景触发描述（LLM 从任务性质匹配，不依赖用户原话） |
| P0 | BIG_OTTER.md "你是谁"与"召唤小獭"两处冗余 | 删除"你是谁"中的召唤描述，统一收敛 |
| P1 | SMALL_OTTER.md "完成任务时"标题层次错误（同级 `##`） | 改为 `###`，作为"你的边界"子段落 |
| P1 | 场景表缺"调试/排查" | 增加排查獭场景 |
| P2 | systemPrompt 模板缺具体示例 | 增加完整示例 |
| P2 | 多轮协作缺发言石路由细节和断裂恢复 | 增加路由说明和恢复策略 |

**用户决策**：触发机制优化 skill description 即可，不在身份层堆砌触发指令。理由："这本来就是 LLM 的能力之一，不需要污染系统提示词。LLM 变强后不存在匹配不上的问题。"

### 第二轮（agent 检视）：需修改

第一轮 6 个修复全部验证通过。新发现 5 个问题（1 中 + 4 低）：

| 严重度 | 问题 | 修复 |
|--------|------|------|
| 中 | 协作文档遗漏 `'user'` 路由目标，"单播"措辞不准确 | 修正路由说明：补充 `'user'` 选项，删除"单播"措辞 |
| 低 | BIG_OTTER.md 删后逻辑衔接略突兀 | 改为"简单的事你直接上手做；复杂的事你也有办法——小獭是你的延伸" |
| 低 | 改动清单 BIG_OTTER.md 行数标注不准 | 修正为 +4 行 |
| 低 | SKILL.md 未说明 systemPrompt 与身份层叠加关系 | 新增"systemPrompt 与身份层的关系"段落 |
| 低 | collaboration-patterns.md 缺排查类单轮协作说明 | 新增"排查类任务"段落 |

### 第三轮（agent 检视）：通过

第二轮 5 项修复全部验证通过。文件间一致性良好，特性文档与实际文件一致。三个边界场景（误召唤、忽略边界、路由歧义）均有合理防护。

发现 1 个低严重度问题（链条断裂恢复缺"目标已解散"场景），已修复。

### 第四轮（独立全新检视）：需修改

从零开始的独立检视。5 个维度：设计自洽性通过、特性文档质量通过、边界与风险通过。可执行性和完整性发现 3 个问题：

| 严重度 | 问题 | 修复 |
|--------|------|------|
| P0 | skill description 缺中文触发短语 | 验证发现已包含中文短语（"帮我分析一下这个需求"等），实际无需修复 |
| P1 | "代码实现"场景"期望可运行代码"与小獭只读矛盾 | 改为"期望代码实现方案和关键代码片段（小獭只读，实际写入由大獭执行）" |
| P2 | 断裂恢复缺 dispatcher 静默丢弃说明 | 补充"发言石投递时目标已离场"场景 |

第二轮 5 项修复全部验证通过。文件间一致性良好，特性文档与实际文件一致。三个边界场景（误召唤、忽略边界、路由歧义）均有合理防护。

发现 1 个低严重度问题（链条断裂恢复缺"目标已解散"场景），已修复。
