/** settingsRepo 持久化 key 集中定义 */

/** 默认模型 alias 覆盖值（settings 页切换默认模型，覆盖 config.yaml 的 llm.default） */
export const DEFAULT_MODEL_ALIAS_KEY = "llm.defaultModelAlias";

/** 用户显示名（海獭称呼搭档用的名字，空值回退到"搭档"） */
export const USER_DISPLAY_NAME_KEY = "user.displayName";
