[F20260923draft][web][BugFix] 对抗审视 delta：S1 修复（debounce 窗口内卸载复活）

检视獭发现：saveDraft('') 空串写入排进 300ms debounce，窗口内卸载时
cleanup 取消 timer + 空值跳过写入且不 removeItem → 旧 key 留存复活。

修补：saveDraft 空串时同步 removeItem（与 clearDraft 完全同构），
手动清空语义=立即删除，不等 debounce。

连带处置 R1：测试注释「空串不写入」与行为相反，已纠正。

新增 S1 反例测试 CE-1（窗口内卸载）+ CE-2（窗口内 beforeunload）。

Modification-Class: narrow-fix

🤖 Generated with [Otter Buddy](https://github.com/chenlaicai/otter-buddy) by 大獭
